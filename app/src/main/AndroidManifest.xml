<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jogo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/prisoner"
        android:label="@string/app_name"
        android:roundIcon="@drawable/prisoner"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WinActivity"></activity>
        <activity android:name=".CustomDalog" />
        <activity android:name=".JogoActivity" />
        <activity android:name=".Placar" />
        <activity android:name=".Sobre" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>